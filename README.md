Welcome, follow the steps below to run locally:

1. Clone the repo to your local machine
2. cd into the repo and install the dependencies:
   -express
   -axios
   -nodemon
   -dotenv
3. Then run it with "npm run dev"

